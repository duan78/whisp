# ==============================================================================
# Whisp Assistant - Dépendances Mises à Jour (Version 2.0)
# ==============================================================================
# Dernière mise à jour: 2025
# Ce fichier contient toutes les dépendances nécessaires pour Whisp Assistant
# ==============================================================================

# ------------------------------------------------------------------------------
# Dépendances de base (Mises à jour critiques)
# ------------------------------------------------------------------------------
pyautogui>=0.9.54
SpeechRecognition>=3.10.0
pyttsx3>=2.90
gTTS>=2.5.0
edge-tts>=6.1.0
pygame>=2.5.0
TTS>=0.22.0
piper-tts>=1.2.0

# ------------------------------------------------------------------------------
# Framework web (Flask 3.0 - Mise à jour majeure)
# ------------------------------------------------------------------------------
flask>=3.0.0
flask-cors>=4.0.0
werkzeug>=3.0.0

# ------------------------------------------------------------------------------
# Traitement audio et données (NumPy 1.26 - Mise à jour critique)
# ------------------------------------------------------------------------------
numpy>=1.26.0
scipy>=1.11.0
Pillow>=10.0.0

# L'optimisation JIT Numba est optionnelle (fallback automatique vers les
# fonctions standards si non installé). Voir requirements_optional.txt.

# ------------------------------------------------------------------------------
# IA et API
# ------------------------------------------------------------------------------
mistralai>=1.9.0
openai>=1.0.0  # Ajouté pour support OpenAI

# ------------------------------------------------------------------------------
# OCR et traitement d'images
# ------------------------------------------------------------------------------
pytesseract>=0.3.13

# ------------------------------------------------------------------------------
# Reconnaissance vocale offline
# ------------------------------------------------------------------------------
vosk>=0.3.45
faster-whisper>=1.0.3  # Alternative plus rapide que whisper original

# ------------------------------------------------------------------------------
# Utilitaires système
# ------------------------------------------------------------------------------
keyboard>=0.13.5
psutil>=5.9.0
plyer>=2.1.0

# ------------------------------------------------------------------------------
# Sécurité (Ajouté pour la modernisation)
# ------------------------------------------------------------------------------
cryptography>=41.0.0  # Pour le chiffrement des clés API

# Les outils de développement (pytest, black, flake8, mypy, ...) sont dans
# requirements-dev.txt et pyproject.toml [project.optional-dependencies] dev.

# ------------------------------------------------------------------------------
# Dépendances conditionnelles selon l'OS
# ------------------------------------------------------------------------------
# Windows
pywin32>=228; platform_system == "Windows"
mouse>=0.7.1; platform_system == "Windows"

# macOS
pyobjc>=7.3; platform_system == "Darwin"
pyobjc-framework-Quartz>=7.3; platform_system == "Darwin"
pyobjc-framework-Cocoa>=7.3; platform_system == "Darwin"

# Linux
python-xlib>=0.31; platform_system == "Linux"
ewmh>=0.1.6; platform_system == "Linux"  # Pour window management
